# Build outputs
target/

# IDE
.vscode/
.idea/
.project
.classpath
.factorypath
.settings/
*.iml

# OS
.DS_Store

# Home-relative caches a tool leaks into the repo root when run with the repo as its cwd
.cache/
.m2/
.local/

# Where docker/Dockerfile's `--output type=local,dest=out` writes the binary and its SBOM
out/
